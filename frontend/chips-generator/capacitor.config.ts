import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'chips-generator',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
